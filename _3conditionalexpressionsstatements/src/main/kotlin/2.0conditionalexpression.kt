fun main() {
    var goodGrade = 95 > 90 // 95 > 90 is the conditional expression
    println(goodGrade)

    var grade = 95
    var goodGrade2 = grade > 90
    println(goodGrade2)

    // The right side of this assignment evaluates to an int because the operator is +
    var grade2 = 10 + 80
    // The right side of this assignment evaluates to a boolean because the operator is >
    var goodGrade3 = grade2 > 90

    // <, >, <=, >=, ==, and !=
}